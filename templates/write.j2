<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Blackit. write</title>
        <link href="{{ url_for('static', filename='write.css') }}" rel="stylesheet" type="text/css">

        <link href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
        <link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.js"></script>
        
        <link rel="shortcut icon" href="">

        <style>
            html {margin: 0; height: 100%;}
            body {margin: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; height: 100%; color: white; background-color: rgb(20, 20, 20);}
            .note-editable {color: black}
            a:hover {text-decoration: none; background-color: rgb(65, 65, 65); cursor: pointer; color: white;}
        </style>
    </head>

    <body>
        <div>
            <p class='top-black-bar'>This website might use cookies, i don't know yet.. I mean their delicious tough...</p>
        </div>

        <header>
            <div class='header-top'>
                <div class='logo'>
                    <h1>Blackit.</h1>
                    <h2>It's like Reddit, but cheaper.</h2>
                </div>

                <div class='log'>
                    <a class='log-button' href="{{ url_for('sort_rating') }}">
                        <p class='log-button-text'>Home</p>
                    </a>
                    <a class='log-button' href="{{ url_for('logout') }}">
                        <p class='log-button-text'>Sign Out</p>
                    </a>
                </div>
            </div>
        </header>

        <div class='banner'>
            <p class='banner-text'>Welcome back to blackit.</p>
        </div>

        <main>
            <div>
                <form action="" method="POST">
                    
                    <div class='write-form'>
                        <div class='title-box'>
                            <label for="Title">Title:</label>
                            <input class='text-input' type="text" id="title" name="title">
                        </div>

                        <div class='content-box'>
                            <label for="password">What's on your mind ?</label>
                            <textarea id="summernote" name="editordata" type="text" id="content"></textarea>
                            <script>
                                $(document).ready(function() {
                                    $('#summernote').summernote();
                                });
                            </script>
                        </div>
                    </div>

                    <div class='post-button'>
                        <input class="submit" type="submit" value="post">
                    </div>

                </form>
            </div>
        </main>
    </body>
</html>